{% extends 'base.html.twig' %}

{% block title %}
    Edit Shoe
{% endblock %}

{% block body %}
    <div class="d-flex align-items-center pt-3 ps-3 gap-4">
        <h1>
            Edit Shoe
        </h1>
        <a href="{{ path('app_admin_shoe_index') }}">
            back to list
        </a>
    </div>
    <div class="container container-global my-5 p-3">
        {{ include('admin/shoe/_formEdit.html.twig', {'button_label': 'Update'}) }}
        {{ include('admin/shoe/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% block importmap %}
    {{ importmap(['app', 'addFormPrices']) }}
{% endblock %}
