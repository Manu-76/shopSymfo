<form method="post" action="{{ path('app_admin_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Etes-vous certain(e) de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger mt-4">Supprimer</button>
</form>
